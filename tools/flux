#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
FLUX="$ROOT/target/debug/flux"

if [[ ! -x "$FLUX" ]]; then
  cargo build --quiet --manifest-path "$ROOT/Cargo.toml" --bin flux
fi

exec "$FLUX" "$@"
