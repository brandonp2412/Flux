fn main() -> i64 {
    let message: str = """
        Flux multiline strings
        keep source indentation readable.
        # stays literal inside the string.
    """
    print(message)
    return 0
}
